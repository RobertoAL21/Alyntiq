Phase 25 — CI/CD

Pull Requests

Run:

* lint
* unit tests
* integration tests
* feature tests
* leakage tests
* backtesting sanity tests
* Docker builds

Main Branch

Potentially:

* build
* push image
* deploy

Add safety checks:

* no live credentials
* no leakage
* model version required
* paper environment enforcement